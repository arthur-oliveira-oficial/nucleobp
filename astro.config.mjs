// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  // Habilita processamento de CSS com PostCSS e TailwindCSS
  // O Astro já suporta PostCSS e TailwindCSS por padrão quando configurados corretamente
});
